
0. installing Kosli CLI from https://github.com/kosli-dev/cli/releases/download/v2.15.3/kosli_2.15.3_linux_amd64.tar.gz ...
   Unexpected HTTP response: 504

1. In the CLI docker references with both a tag and a digest are not supported
   You can't do a kosli-attest with an artifact whose name is
   cyber-dojo/saver:5d42c57@sha256:DIGEST
   You have to use
   cyber-dojo/saver:5d42c57
   which feels wrong.

2. In the artifact_snyk_test.yml the attestation in the callers
   context needs you to checkout their commit. That is tricky
   because you also need the rego file from the snyk-scanning repo.

3. kosli begin trail:
   - has to have a git repo under it
   - does not auto-create the flow

4. Should failed compliance cause non-zero exit code from the workflow?
   I think yes.

5. If attest_to_kosli: false
   then the inner kosli-attest commands will still run.
   What should this case do?
   Return the result of snyk-container-test?
   That was run against an empy .snyk file.

6. Need to tag the workflows in snyk-scanning and for calls, do
       uses: ./.github/workflows/artifact_snyk_test.yml@v2

7. Add decent github step summaries.
   Might be some nice options to print the individual vuln info.


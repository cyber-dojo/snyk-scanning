
0. Very easy to say compliant when it was in fact non-compliant.
   See rego-undefined-field-inviolations.md
   I feel the safest option is to assume non-compliance and prove compliance.

1. The CLI does not support docker image names with both a tag and a digest
   You can't do a kosli-attest with an artifact whose name is
   cyber-dojo/saver:5d42c57@sha256:DIGEST
   You have to use
   cyber-dojo/saver:5d42c57
   which feels wrong.

2. In the artifact_snyk_test.yml the attestation in the callers
   context needs you to checkout their commit. That is tricky
   because you also need the rego file from the snyk-scanning repo.

3. kosli begin trail:
   - has to have a git repo under it
   - does not auto-create the flow

4. If attest_to_kosli: false
   then the inner kosli-attest commands will still run.
   Should they?
   What should this case do?
   Return the result of snyk-container-test?
   That was run against an empty .snyk file.
   Run it against the original .snyk file?
   Or treat the flag as not-attesting as relating only to the OUTER attest.
   Still do the inner attestations. And use them in a kosli-evaluate
   and return the result of that. In this case it is VITAL that these
   inner attestations are trail-level and we dont do an attestation 
   connecting to the fingerprint.   

5. github step summaries.
   Might be some nice options to print the individual vuln info.

6. I would like to know which of the inner attestations 
   caused a kosli-evaluate result of non-compliant.
   Suppose I have 12 inner-attestations --annotate links 
   and only one is actual causing the non-compliance?
   This is why I have used N x kosli-evaluate-trail 
   instead of 1 x kosli-evaluate-trails

7. Really want kosli-attest to have --output=json option which 
   includes the url of the just created attestation.
   Stumbled on this several times.

8. Had to do kosli-attest-artifact and again that felt wrong.

